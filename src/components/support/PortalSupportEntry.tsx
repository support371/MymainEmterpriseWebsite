'use client';

import SupportLauncher from './SupportLauncher';

/**
 * Entry point for support inside the portal.
 * Mounted on the dashboard page for authenticated users.
 */
export default function PortalSupportEntry() {
  return <SupportLauncher />;
}
